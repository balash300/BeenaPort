server:
  port: 8082
  servlet:
    context-path: /api/v1/billing

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL_BILLING}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET}
  system-token-expiry: 3600

property:
  service:
    url: ${PROPERTY_SERVICE_URL}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true

logging:
  level:
    root: WARN
    az.beenaport: INFO